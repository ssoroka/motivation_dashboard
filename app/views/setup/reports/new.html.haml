%h2
  Report Type

- form_for [:setup, @data_source, @data_set, @report] do |f|
  %ul
    %li= @config_info[:description]
    = render :partial => 'shared/custom_config_fields', :object => {:config_info => @config_info}
    %li
      = f.submit 'Next'
